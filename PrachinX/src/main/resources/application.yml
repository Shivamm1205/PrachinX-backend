server:
  port: 8080

spring:
  application:
    name: PrachinX
  datasource:
    url: jdbc:postgresql://ep-frosty-sky-aio9t8ks-pooler.c-4.us-east-1.aws.neon.tech:5432/neondb?sslmode=require&channel_binding=disable
    username: neondb_owner
    password: npg_NKVkptZch5y8
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          batch_size: 20
          fetch_size: 50
  jackson:
    serialization:
      write-dates-as-timestamps: false

jwt:
  secret: praXSecretKey2024VeryLongSecretKeyForJWTTokenGeneration
  expiration: 86400000

ai:
  engine:
    url: http://localhost:8000

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method

logging:
  level:
    com.trading: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
